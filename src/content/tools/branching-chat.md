---
title: "Branching Canvas Conversation"
description: "A bring-your-own-key chat laid out spatially on a canvas: every message is a draggable node in a conversation tree, and you can branch off any message into a parallel thread. Sending from a node uses the path from the root to that node as its context. Toggle between the canvas and a linear chat view. Vanilla pan/zoom canvas, Anthropic-only, key stays in the browser. One file, no build step."
descriptionLevels:
  aware: "A chat drawn as a tree on a pan/zoom canvas. Each message is a node; branch off any message into a parallel thread, and the reply uses the root-to-node path as its context. Toggle a linear view. Bring your own Anthropic key — it stays in your browser."
  plain: "Normally a chat is one straight line. Here it's a tree you can see: every message is a card on a canvas, and you can split off from any point to try a different direction without losing the first one. You can also flip to a normal top-to-bottom view. You paste your own Anthropic key and it never leaves your browser."
href: "/tools/branching-chat.html"
date: 2026-07-21
tags: [ai, canvas, conversation, branching, byo-key]
---
