# Engine Backlog

The runner picks the **first unchecked `- [ ]` item** each cycle. Human-editable between cycles.

**Convention:** only `- [ ]` / `- [x]` lines are tasks. Anything under **Later** is a plain
bullet (no checkbox) so the runner will NOT pick it — promote it to `- [ ]` when it's ready.

**Auto-zone vs gated — mind the guard allowlist.** A task is only truly "auto-zone"
(auto-merges on green CI) if **every** file it touches is inside the guard allowlist:
`src/content/lab/*`, `engine/*`, `src/lib/*`. A task that must edit a **root or config
file outside that list** — `.gitignore`, `package.json`, `tsconfig.json`,
`astro.config.mjs`, `.github/**`, etc. — will be labelled **needs-human** and wait for
Wolf's manual merge, even if the rest of the diff is in-zone. So mark such a task
**GATED** up front rather than "auto-zone." (Learned from Tier 15: the in-code lock was
queued auto-zone but had to add `engine/.run.lock` to the **root** `.gitignore`, so the
guard gated PR #38.)

## Done

- [x] Build the sanitization filter — implement `src/lib/sanitize.ts` so `npm test` passes (allowlist + fail-closed; do not weaken the seeded tests)

## Tier 1 — Harden the loop (do these first; make every later cycle trustworthy)

- [x] Add an independent verify gate to engine/run-cycle.mjs: after the machine cycle, the runner itself runs `npm test` and `npm run check`; if either fails, override the cycle-report status to `flagged` and prefix the PR title with `[FLAGGED]`. Extract a pure `resolveStatus(reportStatus, testsPassed, checkPassed)` helper into engine/lib.mjs and unit-test it in engine/lib.test.mjs.
- [x] Fix currentGhUser() in engine/run-cycle.mjs for gh 2.45: do not use the unsupported `gh auth status --active` flag; instead parse `gh auth status` output for the account marked active. Extract the parsing into a pure helper in engine/lib.mjs and unit-test it against sample gh output.
- [x] Harden the sanitizer secret-scan in src/lib/sanitize.ts against JSON-escaping: scan each allowlisted field's raw string value for registered secrets rather than only JSON.stringify(out), so a secret containing quotes or backslashes cannot evade. Add tests with secrets containing `"` and `\`. Do not weaken the existing seeded tests.
- [x] Quote unsafe tags in renderLabEntry (engine/lib.mjs): a tag that is purely numeric or a YAML reserved word (true/false/null/yes/no/~) must be quoted so it stays a string and satisfies z.array(z.string()) at build time. Add a unit test.
- [x] Make engine/run-cycle.mjs re-runnable: if branch lab/<slug> already exists, reuse or recreate it cleanly instead of crashing; on any cycle failure, check the working tree back out to main. Update engine/README.md to note the behavior.

## Tier 2 — Wire the public/private pipeline (bridge the sanitizer to real experiments)

- [x] Add engine/lib.mjs helper publicEntryFromReport(privateReport) that runs sanitize() then renderLabEntry() to produce a public lab entry, throwing (fail-closed) if sanitization fails. Unit-test with a clean report and a leaky report.
- [x] Encode the direct-vs-review gate: add a pure per-type policy (monitor/experiment publish direct; briefing or opinion content sets draft:true) applied when the runner writes the lab entry, and wire it into engine/run-cycle.mjs. Unit-test the policy function.

## Tier 3 — First experiment: Agent Weekly (plan: docs/superpowers/plans/2026-07-17-agent-weekly-experiment.md)

- [x] Add `digest` to the direct-publish policy in engine/lib.mjs so `draftForType('digest')` returns false (digests publish direct, not draft). Add a unit test.
- [x] Extract the commit/push/PR/account-restore machinery from engine/run-cycle.mjs section 7 into a reusable `publishBranch({repoDir, branch, commitMsg, prTitle, prBody, ghUser, dry})` in a new engine/publish.mjs, and rewire run-cycle.mjs to call it. Preserve dry-run (zero side effects) and the gh-account restore + warning. Verify `node engine/run-cycle.mjs --dry-run` is unchanged and tests pass.
- [x] Build the Agent Weekly experiment: engine/experiments/agent-weekly.md (the research prompt — weekly AI-agent digest per the spec's sources and format, real-link hard rule, writes engine/.experiment-report.json as {status,summary,tags,body}) and engine/run-experiment.mjs <name> (kill-switch + sync main, invoke `claude -p` with the prompt, render a `type: digest` lab entry via renderLabEntry with draft:false, write src/content/lab/<date>-<name>.md, open a PR via publishBranch — no backlog check-off). Support --dry-run (no side effects), gitignore engine/.experiment-report.json + engine/experiment.log, document the Sunday-07:00 cron in engine/README.md. Verify the dry run previews a valid digest entry.

## Tier 4 — Engine hardening

- [x] Make engine/run-experiment.mjs resilient to an existing remote branch: a same-day re-run collides on the date-based branch name (`git push` non-fast-forward, cycle fails). Force-push the throwaway experiment branch (it's fully regenerated from main each run, so nothing is lost), or append a short unique suffix to the branch name. Preserve `--dry-run` (no side effects) and note the behavior in engine/README.md.

## Tier 5 — Site Health monitor (plan: docs/superpowers/plans/2026-07-18-site-health-monitor.md)

- [x] Extract the pure sanitizer logic from src/lib/sanitize.ts into a new plain-JS src/lib/sanitize.core.mjs (so engine .mjs code can import it), and make src/lib/sanitize.ts re-export it with the existing TypeScript types. The existing src/lib/sanitize.test.ts and `npm run check` must stay green — do not change behavior.
- [x] Build engine/probes/site-health.mjs exporting `async probe(target)` that deterministically audits a URL with Node fetch + node:tls and returns Findings {routes:[{path,status,ttfbMs}], ssl:{daysToExpiry,validTo}, brokenLinks:[{url,status}], headers:{csp,hsts,xcto}, pageWeight:{htmlBytes,assetCount}}. Check routes /, /lab, /work, /writing, /rss.xml; crawl homepage links and flag non-2xx; read SSL expiry via node:tls. Unit-test the aggregation with mocked fetch/tls in engine/probes/site-health.test.mjs.
- [x] Write engine/experiments/site-health.md — the monitor operating manual. Given deterministic Findings for whatupwolf.com, the machine writes a PrivateReport {meta:{urls,secrets}, findings, public:{title,summary,body,tags}} to engine/.experiment-report.json using ONLY the probe numbers (never invent). All specific routes/URLs/dates go in meta/findings and NEVER in public; the public block proves issues were found and flagged without the exploitable specifics; status:"flagged" if the probe failed.
- [x] Add a per-experiment `kind` (digest|monitor) to engine/run-experiment.mjs with registry entry {'site-health':{kind:'monitor',type:'monitor',titlePrefix:'Site Health',target:'https://whatupwolf.com'}}. For kind monitor: run probe(target) from engine/probes/site-health.mjs, invoke claude -p with engine/experiments/site-health.md + the Findings, parse the PrivateReport, write it to engine/reports/<date>.json, then import sanitize from src/lib/sanitize.core.mjs and use publicEntryFromReport(report,{sanitize,date,type:'monitor'}) to render the Lab entry, and publishBranch. Keep kind digest as-is. Add engine/reports/ and engine/.experiment-report.json to engine/.gitignore. Verify `node engine/run-experiment.mjs site-health --dry-run` previews a valid type:monitor entry with no side effects.
- [x] Create engine/run-health.sh (mirror engine/run-weekly.sh: source nvm, cd repo root, exec node engine/run-experiment.mjs site-health >> engine/experiment.log 2>&1). Update engine/README.md Cron section with the canonical Wednesday 07:00 line `0 7 * * 3 /home/wolf/whatupwolf/engine/run-health.sh` and the same "point cron at the wrapper" rule.

## Tier 6 — Site fixes (GATED — these touch the site, so the PR is needs-human / Wolf merges)

<!-- CONVENTION: a gated item stays [x] once BUILT (its needs-human PR is open) so the auto-loop
     doesn't re-pick it and collide with the open PR's branch. It's genuinely done from the
     engine's side; only Wolf's merge is pending. -->
- [x] (BUILT — PR #22 open, awaiting Wolf's merge) Close the Site Health audit's security-header finding: add a `public/_headers` file so Cloudflare serves security headers on all routes — `Strict-Transport-Security` (max-age >= 15552000; includeSubDomains), `X-Content-Type-Options: nosniff`, `X-Frame-Options: SAMEORIGIN`, `Referrer-Policy: strict-origin-when-cross-origin`, and a `Content-Security-Policy`. CRITICAL: the site is Astro + React islands (hydration scripts) + Tailwind, and a wrong CSP breaks the LIVE site at runtime in a way CI cannot catch — so ship the CSP as `Content-Security-Policy-Report-Only` first (report, do not enforce), or a conservative policy that allows 'self' plus exactly the script/style sources the build emits. Do NOT break the site. Verify `npm run build` still succeeds. (This PR touches `public/` — a gated path — so it will be labelled needs-human for Wolf's review; that is intended.)

## Tier 7 — Loop hardening + prober hygiene + a build experiment

- [x] Make the self-building loop skip a backlog item whose branch already has an open PR, so a gated needs-human PR left unmerged no longer blocks the loop (re-pick + branch collision). In engine/run-cycle.mjs, after computing each candidate item's branch `lab/<slug>`, check `gh pr list --head <branch> --state open`; if a PR already exists, skip to the next unchecked item instead of rebuilding. Extract a pure helper (given items + a set of taken branch names, return the next buildable item) and unit-test it in engine/lib.test.mjs. Preserve `--dry-run` (no side effects).
- [x] Add good-citizen defaults to engine/probes/site-health.mjs so the monitor is safe-by-construction if ever pointed at a permitted third-party site (see the legal discussion). On every request send an identifying User-Agent (e.g. `whatupwolf-site-health/1.0 (+https://whatupwolf.com)`); fetch and respect robots.txt (skip Disallowed paths for our UA); and rate-limit (a small delay between fetches and/or a low concurrency cap). Keep whatupwolf.com behavior working. Add unit tests for the UA header, robots.txt parsing/skip, and the rate limiter. (engine/ — auto-zone.)
- [x] Research and build a first prototype of a better recipe/cooking tool (dogfood: Wolf makes recipes with Claude's help but doesn't love Claude's "cooking mode" as-is). FIRST research (WebSearch) what Claude cooking mode does and the common pain points of cooking from a screen — hands-free/tap step progression, one-step-at-a-time focus, per-step timers, ingredient scaling, unit conversion, keeping the screen awake (wake-lock), maybe voice. THEN build a SELF-CONTAINED single-file tool at `public/tools/cooking.html` — no build step, no external deps, works offline — that lets you paste or enter a recipe and cook through it with a genuinely better UX targeting those pain points (big step-at-a-time view, next/prev by tap or keyboard, per-step timers, ingredient scaling, wake-lock). Write a `type: experiment` Lab entry describing the concept, the pain points it targets, and linking `/tools/cooking.html`. Tasteful, working v1 — a starting point to iterate, not final. (Touches `public/` — GATED, so the PR is needs-human for Wolf's review.)

## Tier 8 — Site polish (Lab UX, nav, dedup)

- [x] De-duplicate the Cook Mode Lab entries: PR #25 left TWO entries for the same tool — the dedicated writeup `src/content/lab/2026-07-18-cook-mode-a-self-contained-recipe-reader-for-messy-hands.md` and the generic build-log `src/content/lab/2026-07-18-research-and-build-a-first-prototype-of-a-better-recipe-cook.md`. Keep the dedicated "Cook Mode" writeup, delete the generic build-log one, so Cook Mode appears ONCE in the Lab feed. Verify `npm run build` still succeeds. (src/content/lab — auto-zone.)
- [x] (SUPERSEDED by Tier 9 / PR #30 — the Tools nav link is now part of the /tools listing feature) Add the Tools / Cook Mode link to the site nav in src/components/Nav.astro so the tool at `/tools/cooking.html` is reachable from navigation. Match the existing nav style (paper theme, mono chrome). Verify `npm run build`. (Touches src/components — GATED, needs-human.)
- [x] Revamp the Lab page filter UX (src/components/LabFilter.tsx + its use in src/pages/lab/index.astro): the per-tag buttons have gotten out of hand as entries pile up. Replace them with a simpler selector — a dropdown/select to filter by tag (and/or type) — AND add a text search box that filters entries by title + summary (client-side, keep the existing React-island approach, fast, accessible, on-brand). Verify `npm run build`. (Touches src/components / src/pages — GATED, needs-human.)

## Tier 9 — Tools home + Lab→tool links (supersedes closed PR #27)

- [x] Build a Tools home and link Lab entries to their tools. FOUR parts: (a) Add a `tools` content collection in src/content.config.ts — schema `{ title: string, description: string, href: string (the live tool URL, e.g. /tools/cooking.html), date: coerce.date, tags: array(string).default([]) }` — and create the first entry src/content/tools/cook-mode.md (title "Cook Mode", a short description of the recipe reader, href "/tools/cooking.html", date, tags [cooking]). (b) Add a `/tools` listing page at src/pages/tools/index.astro that renders the tools collection as cards (title, description, and an "Open →" link to each `href`), matching the existing listing pages — study src/pages/work.astro and src/pages/writing.astro and reuse the paper theme + mono-chrome styling and the Base layout. (c) In src/components/Nav.astro add a "Tools" link → /tools, matching the existing nav-link style (do NOT add a per-tool link). (d) Add an optional `tool: z.string().optional()` field to the `lab` collection schema, render a prominent "Try it →" link to that URL wherever a lab entry is displayed (src/components/LabEntry.astro and/or src/pages/lab/[...id].astro), and set `tool: /tools/cooking.html` in the frontmatter of src/content/lab/2026-07-18-cook-mode-a-self-contained-recipe-reader-for-messy-hands.md. Verify `npm run check` (0 errors) and `npm run build` both pass. (Touches src/pages, src/components, src/content.config.ts — GATED, needs-human.)

## Tier 10 — Loop hardening (closed/merged PRs too)

- [x] Extend loop-skip so it skips a backlog item whose branch has ANY existing PR — open, closed, OR merged — not just open. Today `branchHasOpenPr` in engine/run-cycle.mjs uses `--state open`, so a CLOSED (superseded) gated PR leaves its item unchecked with no open PR and the loop re-picks + rebuilds it (this happened with PR #27→#29). Change the check to `gh pr list --head <branch> --state all` (rename to `branchHasPr` or similar) so once an item has been built into a PR it is never rebuilt. Update `pickBuildableItem`'s usage and the unit tests accordingly. Preserve `--dry-run`. (engine/ — auto-zone.)

## Tier 11 — Visitor-selectable "tech level" (ambitious; take the turns you need)

- [x] Build a visitor-selectable **tech level** for the whole site. A control lets a visitor pick one of THREE levels and the site's content "translates" to be genuinely valuable at that level: (1) **Technical** — the current deep content, for engineers who want the details; (2) **Tech-aware** — understands tech speak but doesn't want deep details: concise, competent, lighter; (3) **Non-technical** — likely most clients: high-level, plain-language, conceptual summaries that convey the real value without jargon. GOAL: whatever level a visitor chooses, the site reads as if written for them and still conveys the value of the work.
  The HOW is YOURS to design — research it first, then choose the mechanism, and assess what details/pieces belong at each level. HARD CONSTRAINTS: this is a STATIC Astro site on Cloudflare — there is NO server and NO per-visitor LLM call, so the three levels must be authored or PRE-GENERATED and stored (schema field / structured content / data), and switched CLIENT-SIDE (do not add a runtime translation API). The level toggle lives in the site chrome (nav/header), is a clear 3-way control, persists the choice (localStorage), and applies site-wide. Stay on-brand: paper theme, mono chrome, tasteful, fast, accessible, minimal JS. `npm run check` (0 errors) and `npm run build` must pass and the site must work at every level.
  APPROACH — take as many turns of research, development, and self-evaluation as you need: research audience-tiered / progressive-disclosure content patterns and how to store+switch content variants statically; design a clean content model for the 3 levels and generate the tech-aware + non-technical versions from the technical source yourself (you are an LLM — write them and store the results); apply it to the KEY visitor-facing surfaces (Home, Work, the Lab feed summaries, Now/About — you choose the right scope for a strong v1; you need not translate every deep Lab entry); then SELF-EVALUATE by reading the site at each level as that audience would, and refine until each level genuinely reads well. Deliver a strong, coherent, working v1. (Touches the site broadly — pages, components, layout, content, config — GATED, needs-human; make it a tasteful, reviewable whole.)

## Tier 12 — LLM-interaction research phase (research FIRST, build gated on findings; design: docs/superpowers/specs/2026-07-20-llm-interaction-research-phase-design.md)

<!-- Both items are engine/** (auto-zone). They build EXPERIMENT infrastructure — the
     research itself then runs via engine/run-experiment.mjs (manually or by cron), like
     Agent Weekly / Site Health. Nothing here queues a prototype: prototypes are gated on
     the sprint's shortlist (see Later). Order matters — the sprint (A) runs before the
     standing digest (B). -->

- [x] Build the **Interaction Landscape** research sprint — a one-shot experiment that surveys the landscape of *novel ways of interacting with LLMs* and ends with a ranked prototype shortlist Wolf reviews. (a) Add the registry entry `'interaction-landscape': { kind: 'digest', type: 'briefing', titlePrefix: 'Interaction Landscape' }` to `EXPERIMENTS` in engine/run-experiment.mjs. (b) Write engine/experiments/interaction-landscape.md — the operating manual, mirroring engine/experiments/agent-weekly.md's discipline (discovery-first broad WebSearch, worldwide sources, **verify every link**, factual machine-log voice, real fetched links only, no hype) but aimed at **interaction paradigms, not releases**: voice / ambient agents, generative & streaming UI, memory / context UX, multimodal, computer-use, tool-call & clarification UX, "thinking out loud" surfaces, elicitation patterns, agent-handoff UX. The body MUST end with a ranked `## Prototype Shortlist` where each candidate is `**Name** — what it is · why it's interesting · rough build shape · static-site feasibility (client-only / BYO-key / needs-server)`. Write engine/.experiment-report.json as `{status, summary, tags, body}` (same contract as agent-weekly). (c) Because `type` is `briefing`, ensure `draftForType('briefing')` returns **true** in engine/lib.mjs so the sprint lands as a **draft** Lab entry for Wolf's review (that review is the gate) — add/confirm a unit test in engine/lib.test.mjs. (d) The shared title template in run-experiment.mjs hardcodes `— week of <date>`, which reads wrong for a non-weekly experiment; generalize it (e.g. a per-registry `titleSuffix`, or date-only) so Interaction Landscape titles naturally while Agent Weekly / Site Health output stays sensible and tests stay green. (e) This is **one-shot** (run manually — `node engine/run-experiment.mjs interaction-landscape` — no cron); note that in engine/README.md. Verify `node engine/run-experiment.mjs interaction-landscape --dry-run` previews a valid `type: briefing` DRAFT entry with no side effects, and `npm test` + `npm run check` pass. (engine/ — auto-zone.)
- [x] Build the **Interaction Lab** standing digest — a recurring monthly experiment (mirror Agent Weekly) that keeps the LLM-interaction landscape fresh once the sprint has run. (a) Add the registry entry `'interaction-lab': { kind: 'digest', type: 'digest', titlePrefix: 'Interaction Lab' }` to `EXPERIMENTS` in engine/run-experiment.mjs. (b) Write engine/experiments/interaction-lab.md — a lighter monthly counterpart to interaction-landscape.md: "what's new this month in how people interact with LLMs" — same source-sweep discipline and real-link hard rule, ~5–8 items, factual voice; `type: digest` so it publishes direct (`draftForType('digest')` is already false). (c) Create engine/run-interaction-lab.sh mirroring engine/run-weekly.sh (source nvm, cd repo root, `exec node engine/run-experiment.mjs interaction-lab >> engine/experiment.log 2>&1`). (d) In engine/README.md's Cron section add the canonical **monthly** line `0 7 1 * * /home/wolf/whatupwolf/engine/run-interaction-lab.sh` with the same "point cron at the wrapper, not a pinned node path" rule; note that Wolf installs the cron by hand (as with Agent Weekly / Site Health). Verify `node engine/run-experiment.mjs interaction-lab --dry-run` previews a valid `type: digest` entry with no side effects, and `npm test` + `npm run check` pass. (engine/ — auto-zone.)

## Tier 13 — Experiment-runner framework hygiene (the runner is already the framework; just kill the two remaining repetitions; design: docs/superpowers/specs/2026-07-20-experiment-runner-framework-hygiene-design.md)

<!-- Small, non-speculative extraction. engine/** (auto-zone). Runs AFTER the Tier-12
     experiments exist, so it folds all three named wrappers (weekly, health,
     interaction-lab) into shims at once. Do NOT add a scaffold/generator command. -->

- [x] Extract the experiment registry into a module and collapse the wrapper scripts, without changing runner behavior. (a) Move the `EXPERIMENTS` object (and its explanatory comment) out of engine/run-experiment.mjs into a new engine/experiments/registry.mjs that `export`s it, and import it back into run-experiment.mjs — pure data/logic split, no behavior change; `node engine/run-experiment.mjs --dry-run` for each known experiment and the full `npm test` + `npm run check` must stay green. Add a small unit test (engine/lib.test.mjs or a new engine/experiments/registry.test.mjs) asserting every registry entry is well-formed: `kind` ∈ {digest, monitor}, `type` ∈ the lab enum (experiment|briefing|monitor|note|digest), and every monitor carries a `target`. (b) Create engine/run-experiment.sh — ONE parametric wrapper that takes the experiment name as `$1` (guard against a missing arg), sources nvm, `cd`s to the repo root, and `exec node engine/run-experiment.mjs "$1" >> engine/experiment.log 2>&1` (mirror engine/run-weekly.sh's nvm/robustness comment). Ensure the executable bit is set (git tracks mode). (c) Convert engine/run-weekly.sh, engine/run-health.sh, and engine/run-interaction-lab.sh into one-line shims that `exec "$(dirname "$0")/run-experiment.sh" <name>` (agent-weekly / site-health / interaction-lab respectively), keeping them executable — so the already-installed crontabs keep working with ZERO migration and no silent breakage. (d) Update engine/README.md: the canonical cron form is now `…/engine/run-experiment.sh <name>`; note the named files are backward-compat shims for already-installed crontabs (removable once Wolf re-points crontab, but not required). Do NOT add a scaffold/generator command (speculative at four experiments). (engine/ — auto-zone.)

## Tier 14 — First interaction prototype: Generative UI Canvas (shortlist #1; GATED; design: docs/superpowers/specs/2026-07-20-generative-ui-canvas-prototype-design.md)

<!-- The build phase begins. Promoted from the Interaction Landscape shortlist (#1) after
     Wolf's review. Anthropic-only BYO-key, faithful multi-window canvas. Touches public/**
     + src/content/tools/** + likely public/_headers → GATED / needs-human. -->

- [x] Build the **Generative UI Canvas** — a self-contained BYO-key tool where a typed request makes Claude generate interactive HTML "app windows" on a canvas, and interacting with a generated app posts back and re-invokes the model. FIRST read the design (docs/superpowers/specs/2026-07-20-generative-ui-canvas-prototype-design.md) AND confirm current Anthropic model IDs + the browser-direct calling contract via the `claude-api` skill (do NOT hardcode a model id or header from memory). THEN build a self-contained single-file tool at `public/tools/generative-ui.html` (no build step, no external deps, works offline — like Cook Mode): (a) **Model call** — `fetch` to `https://api.anthropic.com/v1/messages`, streaming SSE (`stream:true`), headers `x-api-key` (the visitor's key), `anthropic-version`, `anthropic-dangerous-direct-browser-access: true`; default model **Claude Sonnet 5** with a small model picker. (b) **Tool loop** — the model holds `create_new_window(title, html)`, `set_window_html(id, html)`, `dom_replace(id, selector, html)`; the page executes each on the canvas, returns a `tool_result`, and a user interaction inside a window `postMessage`s back to re-invoke the model. (c) **Canvas** — hand-rolled **vanilla JS** pan/zoom + draggable window panes (NOT a real React Flow dependency — stay single-file / offline / CSP-clean). (d) **SECURITY (critical, a reviewer will check this):** generated apps render in iframes with `sandbox="allow-scripts"` and **without** `allow-same-origin`, so model-generated code runs but cannot reach the parent, its `localStorage`, or the API key; the key lives ONLY in the parent, is passed to no iframe, and all app↔page comms are `postMessage`. (e) **Trust UX** — prominent "your key stays in your browser, never sent to any server," a get-a-key link, a clear-key button, a "generated code runs sandboxed" note, and graceful missing/invalid-key + API/rate-limit error handling. (f) **CSP** — the site's `public/_headers` CSP needs `https://api.anthropic.com` in `connect-src` before it is enforced or the tool breaks live; add it (keep the rest of the policy intact — do NOT weaken it) and note the coordination in the PR. (g) **Publish** — add a `tools` collection entry `src/content/tools/generative-ui.md` (title, description, `href:/tools/generative-ui.html`, tags) and a `type: experiment` Lab writeup (concept, the generative-UI pattern + precedent, why it matters) with `tool:/tools/generative-ui.html` in frontmatter, matching the Cook Mode / Tools-home conventions. On-brand paper theme + mono chrome for the shell. Verify `npm run check` (0 errors) and `npm run build` pass. (Touches `public/**` + `src/content/tools/**` + `public/_headers` — GATED, needs-human.)

## Tier 15 — Engine self-protection: in-code single-instance lock (auto-zone)

<!-- Queued alongside an hourly run-cycle cron (flock-guarded wrapper). This is the
     durable, in-code version of that guard so the loop self-protects regardless of how
     it's invoked. The memory long listed "single-process + lockfile" as a control that
     never actually existed in code — this builds it. -->

- [x] Add an in-code single-instance lock to engine/run-cycle.mjs AND engine/run-experiment.mjs so two overlapping runs can't corrupt each other's git state. On startup (before any git/network work, after the PAUSED check), acquire an exclusive lock; if it's already held by a live process, log "another run in progress — exiting" and exit 0 WITHOUT touching git; release it on every exit path including the catch/recover. Use a dependency-free **pidfile** approach: write the pid to a gitignored `engine/.run.lock`, and on startup treat the lock as free if the file is missing or its pid is no longer alive (`process.kill(pid, 0)` throws ESRCH) — a stale lock from a killed run must not wedge the loop. Extract the pure decision (given lockfile contents + a liveness probe, is the lock free?) into a testable helper in engine/lib.mjs and unit-test the free / held-by-live / stale-pid cases. Add `engine/.run.lock` to .gitignore. Preserve `--dry-run` (no lock side effects). (engine/ — auto-zone.)

## Tier 16 — Interaction prototype #2: Reasoning-trace A/B (shortlist #2; client-only, zero-key; GATED)

<!-- The safest prototype: no API key, no server, no live model — pre-recorded trace pairs
     as static JSON. An interactive demonstration, not a tool you operate. Grounds the
     "Seeing the Reasoning" finding (arxiv 2603.07306): presentation format didn't move
     trust; reasoning *reliability* did, and people can't easily tell them apart. -->

- [ ] Build the **Reasoning-trace A/B** interactive demo — a self-contained, **client-only** tool at `public/tools/reasoning-trace.html` (no build step, no external deps, **no API key, no server, no live model call** — works fully offline, like Cook Mode). The experience: present a question + a confident final answer with a reasoning trace, ask the visitor to judge whether they trust it; then show a second case in the same style whose trace contains a subtle logical flaw or a fabricated intermediate step yet reaches the same answer; vary the reveal timing of the trace (instant / delayed / on-demand) as a toggle; then reveal the research finding — that presentation format did not significantly change trust while reasoning reliability did, and most people don't catch an unfaithful trace (cite *Seeing the Reasoning*, https://arxiv.org/abs/2603.07306). Author **3–5 example trace pairs** (a faithful trace and a subtly-unfaithful one that lands on the same answer) as **static JSON embedded in the file** — write convincing, self-consistent examples; the flaw must be real but subtle. Track and show the visitor whether they were fooled. On-brand paper theme + mono chrome. Add a `tools` collection entry `src/content/tools/reasoning-trace.md` and a `type: experiment` Lab writeup (the concept, the finding, why it matters for AI literacy) with `tool: /tools/reasoning-trace.html`. Verify `npm run check` (0 errors) + `npm run build`. (Touches `public/**` + `src/content/tools/**` — GATED, needs-human.)

## Tier 17 — Interaction prototype #3: Branching canvas conversation (shortlist #3; BYO-key; GATED)

<!-- CanvasConvo's core move (arxiv 2605.15848) on a static page. Reuses the Generative UI
     Canvas BYO-key posture (Tier 14): Anthropic-only, key in localStorage, browser-direct. -->

- [ ] Build the **Branching Canvas Conversation** tool — a self-contained BYO-key tool at `public/tools/branching-chat.html` where a chat is laid out spatially on a canvas and the visitor can **branch off any message into a parallel thread**, with a toggle between the canvas view and a linear chat view (CanvasConvo, https://arxiv.org/abs/2605.15848). FIRST confirm current Anthropic model IDs + the browser-direct calling contract via the `claude-api` skill (do NOT hardcode from memory), and reuse the Generative UI Canvas security/BYO-key posture (see docs/superpowers/specs/2026-07-20-generative-ui-canvas-prototype-design.md): **Anthropic-only**, `fetch` to `https://api.anthropic.com/v1/messages` streaming SSE with `x-api-key` + `anthropic-dangerous-direct-browser-access: true`, key in `localStorage` and never sent to any server, default **Claude Sonnet 5** + model picker, "your key stays in your browser" messaging + get-a-key link + clear-key button. Build shape: a hand-rolled vanilla-JS canvas (pan/zoom, draggable nodes — NOT React Flow) over a conversation **tree**; sending from a node assembles context as the path from root to that node; branching creates a child thread that diverges from the chosen message. Ensure `https://api.anthropic.com` is in the `public/_headers` CSP `connect-src` (Tier 14 may have already added it — if so, leave it; do not weaken the policy). Add a `tools` collection entry `src/content/tools/branching-chat.md` and a `type: experiment` Lab writeup with `tool: /tools/branching-chat.html`. On-brand paper theme + mono chrome. Verify `npm run check` (0 errors) + `npm run build`. (Touches `public/**` + `src/content/tools/**` + `public/_headers` — GATED, needs-human.)

## Tier 18 — Interaction prototype #4: Memory inspector (shortlist #4; BYO-key; GATED)

<!-- MemoAnalyzer's under-copied idea (arxiv 2410.14931): render memory state as a visual
     property — background shading mapped to inferred confidence and sensitivity. Reuses the
     Tier-14 BYO-key posture. -->

- [x] Build the **Memory Inspector** tool — a self-contained BYO-key tool at `public/tools/memory-inspector.html`: a chat whose **memory store is visible beside it**, where after each turn an extraction pass pulls durable facts into a `localStorage` memory list, entries can be **edited or forgotten inline**, and each entry's **background shading encodes inferred confidence (one axis) and sensitivity (another axis)** so what the system "knows" and how sure/sensitive it is is legible without opening a panel (MemoAnalyzer, https://arxiv.org/abs/2410.14931). FIRST confirm current Anthropic model IDs + the browser-direct contract via the `claude-api` skill (do NOT hardcode from memory), and reuse the Generative UI Canvas security/BYO-key posture (Anthropic-only, `x-api-key` + `anthropic-dangerous-direct-browser-access: true`, key in localStorage never sent to a server, default Claude Sonnet 5 + picker, key-safety messaging + clear-key button). Build shape: stored memory is injected into the model context each turn; a per-turn extraction call updates the memory list (add/update, with a confidence + sensitivity estimate per item driving the shading); edits/forgets take effect on the next turn. Ensure `https://api.anthropic.com` is in the `public/_headers` CSP `connect-src` (Tier 14 may have added it — if so, leave it; do not weaken the policy). Add a `tools` collection entry `src/content/tools/memory-inspector.md` and a `type: experiment` Lab writeup with `tool: /tools/memory-inspector.html`. On-brand paper theme + mono chrome. Verify `npm run check` (0 errors) + `npm run build`. (Touches `public/**` + `src/content/tools/**` + `public/_headers` — GATED, needs-human.)

## Later (not queued — promote to `- [ ]` when ready)

- **Interaction prototype #5: Widget-ified prompting** — the last shortlist candidate. Held because the faithful version is **needs-server** (decoding-time logit modulation, which no hosted API exposes); a static BYO-key version can only approximate it by rewriting the prompt and must be labelled an approximation, not the paper's claim. Revisit if/when the app subdomain lands.
- **App subdomain for live-LLM prototypes (e.g. `app.whatupwolf.com`)** — the ambitious payoff that *lifts the static-only constraint*: a place to host real server-side / per-visitor LLM interaction (BYO-key or a small edge function) that the static Cloudflare site can't. Explicitly deferred until AFTER the research phase; scope it from the shortlist once we know which prototypes actually need a server. Design + infra decision goes through brainstorming first.
- **Agent Weekly schema change (GATED — Wolf merges)** — add `digest` to the lab `type` enum in src/content.config.ts. Handled as a separate hand-merged PR since it touches a gated path; kept out of the auto-queue so a needs-human PR can't block the loop.
<!-- "Experiment-runner framework" promoted to Tier 13 (2026-07-20): the runner was already
     the framework; only the wrappers + inline registry remained to extract. -->

