# Engine run artifacts — machine-local, never committed.

# The private half of a monitor report: exact routes, cert dates, broken-link
# URLs. Archived here for Wolf; only the sanitized public block ever ships.
reports/

# The scratch report a headless run writes, read once and deleted.
.experiment-report.json

# Where the last cycle.log scan finished (engine/log-scan.mjs). A byte offset into
# a machine-local log — meaningless on any other checkout.
.log-scan.json
